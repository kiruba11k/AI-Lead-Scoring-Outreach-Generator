{
  "title": "AI Lead Intelligence & Outreach Agent",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Google Maps Search URL",
      "type": "array",
      "description": "Paste a Google Maps search URL (example: web design companies in Bangalore)",
      "editor": "requestListSources",
      "prefill": [
        {
          "url": "https://www.google.com/maps/search/web+design+companies+in+bangalore"
        }
      ]
    },

    "maxResults": {
      "title": "Max Results to Scrape",
      "type": "integer",
      "description": "Maximum number of businesses to scrape from Google Maps",
      "default": 30,
      "minimum": 1,
      "maximum": 300,
      "editor": "number"
    },

    "services": {
      "title": "Services to Offer",
      "type": "array",
      "description": "Select one or more services to pitch in outreach messages",
      "editor": "select",
      "items": {
        "type": "string",
        "enum": [
          "Web Design",
          "SEO",
          "Digital Marketing",
          "Google Ads",
          "Review Management",
          "Reputation Recovery"
        ]
      },
      "default": ["Web Design"]
    },

    "tone": {
      "title": "Outreach Tone",
      "type": "string",
      "description": "Tone used for AI-generated outreach messages",
      "editor": "select",
      "enum": ["friendly", "formal", "aggressive"],
      "default": "friendly"
    },

    "language": {
      "title": "Outreach Language",
      "type": "string",
      "description": "Language used for WhatsApp and email messages",
      "editor": "select",
      "enum": ["English", "Hindi", "Tamil"],
      "default": "English"
    },

    "groqApiKey": {
      "title": "Groq API Key",
      "type": "string",
      "description": "Groq API key used to generate AI-powered outreach messages",
      "editor": "textfield",
      "isSecret": true
    },

    "useProxy": {
      "title": "Use Apify Proxy",
      "type": "boolean",
      "description": "Enable Apify proxy rotation to reduce blocking and improve scraping reliability",
      "default": true,
      "editor": "checkbox"
    }
  },
  "required": ["startUrls", "groqApiKey"]
}
