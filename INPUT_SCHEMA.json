{
  "title": "AI Lead Intelligence & Outreach Agent",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Google Maps Search URL",
      "type": "array",
      "description": "Paste Google Maps search URL",
      "editor": "requestListSources"
    },
    "maxResults": {
      "title": "Max results to scrape",
      "type": "integer",
      "description": "Maximum number of businesses to scrape",
      "default": 30,
      "minimum": 1,
      "maximum": 300,
      "editor": "number"
    },
    "services": {
      "title": "Services to Offer (Multiple)",
      "type": "array",
      "description": "Select one or more services to pitch during outreach",
      "editor": "select",
      "items": {
        "type": "string",
        "enum": [
          "Web Design",
          "SEO",
          "Digital Marketing",
          "Google Ads",
          "Review Management",
          "Reputation Recovery"
        ]
      },
      "default": ["Web Design"]
    },
    "tone": {
      "title": "Outreach Tone",
      "type": "string",
      "description": "Choose the tone of outreach messages",
      "editor": "select",
      "enum": ["friendly", "formal", "aggressive"],
      "default": "friendly"
    },
    "language": {
      "title": "Outreach Language",
      "type": "string",
      "description": "Language used for WhatsApp and email outreach",
      "editor": "select",
      "enum": ["English", "Hindi", "Tamil"],
      "default": "English"
    },
    "openaiApiKey": {
      "title": "OpenAI API Key (optional)",
      "type": "string",
      "description": "API key used for AI-generated outreach messages and analysis",
      "editor": "textfield"
    },
    "useProxy": {
      "title": "Use Apify Proxy",
      "type": "boolean",
      "description": "Enable Apify proxy rotation to reduce blocking and improve scraping reliability",
      "default": true,
      "editor": "checkbox"
    }
  }
}
